# A PMA robot trajectory point with a simple integer identifier unique to each
# motion performed.

# An identifier, so each point within the same motion can be grouped together
int64 motion_number

# The PMA robot trajectory point
PmaRobotTrajectoryPoint point
